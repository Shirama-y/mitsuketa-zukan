# みつけた図鑑 / わたしの図鑑ライブラリ

写真を撮って、自分だけの図鑑やコレクションをつくるスマホ向けWebアプリです。

植物や生きものだけでなく、レゴブロック、シール、ぬいぐるみ、雑貨、旅先で見つけたものなど、
**本人が「集めたい」と思ったものなら何でも一冊の図鑑にできます。**

現在の `night-library` ブランチでは、女性を主なターゲットとして、
深いネイビーを基調にした「夜の図書館」の世界観へUIを再構築しています。

- トップはタイトルを大きく表示せず、本棚そのものをUIにする
- 本を横にスライドして選ぶ
- 実際に撮影した写真をコレクションにする
- 図鑑ごとにタイトル、フォント、色、表紙素材、装丁、アイコンを変更できる
- 漢字・別名も登録できる
- お気に入り・検索・タグに対応
- データは端末内に保存
- バックアップと復元に対応
- iPhoneのホーム画面からアプリのように開けるPWAを目指す

## 共同制作について

この改修は **菅野・白間の共同制作**として進めています。

二人ともエンジニアではないため、

1. ChatGPTが別ブランチでコードを変更
2. GitHubのPull Requestで確認
3. 見た目や使い方を日本語でコメント
4. 自動チェックを確認
5. 二人で合意してから `main` に反映

という流れを基本にします。

初心者向けの手順は [共同制作のてびき](共同制作のてびき.md) を参照してください。

---

## 元の設計思想

子どもが自分で図鑑をつくり、身のまわりで見つけたものを写真と言葉で記録していくアプリ。
石でも、車でも、花でも、レゴでも、ペットボトルのキャップでもよい。図鑑のタイトルは
子ども自身が決める。

正しい名前を調べる科学図鑑ではなく、**よく見て気づく力を育てる「観察のトレーニング」**
として作っている。DODOLABO の活動で、子どもが実際に触れることを想定している。

設計の判断と、その理由は [CLAUDE.md](CLAUDE.md) にまとめてある。
**コードを触る前に、まずそこを読んでほしい。**

## 中身

```
index.html   アプリ本体。これ1枚で動く。ビルドもサーバーも不要
             （このファイルがそのまま GitHub Pages で配信される）
CLAUDE.md    設計の判断と理由。Claude Code が自動で読み込む
tools/       手元の作業道具（アプリの動作には不要）
```

## 動かす

`index.html` をブラウザで直接開いても、ほとんどの画面は見られる。ただし
**Chrome はローカルファイルから IndexedDB を使わせない**ので、記録の保存を試すときは
簡易サーバー越しに開くこと。

```sh
cd "<このフォルダ>"
python3 -m http.server 8777
# → http://localhost:8777/ を開く
```

## 直すときの約束

守らないと、すでに子どもが作った図鑑が壊れる。

- **`COVERS` / `PATTERNS` / `FRAMES` を並べ替えない。** 配列の順番が、保存されている
  `color` / `theme` / `frame` の番号そのもの。足すときは必ず末尾に
- **番号を詰めない。** 記録を消しても `nextNo` は戻さない。欠番は欠番のまま残す
- **IndexedDB の移行処理を作り直さない。** 既存ユーザーのデータを消さないことが最優先
- **実行時に AI や外部 API を呼ばない。** 運用コストをゼロに保つための制約
- **観察欄と想像欄を統合しない。** 事実と想像を分けて書く練習が、このアプリの核

## 試す

保存まわりを触ったら、実ブラウザでの試験を通すこと。ヘッドレスの仮想時間では
IndexedDB のコールバックが発火しないので、Chrome を DevTools プロトコルで
直接動かしている。

```sh
# 1) アプリを配信しておく
python3 -m http.server 8777

# 2) 別のターミナルで Chrome を起動
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless=new --remote-debugging-port=9333 \
  --user-data-dir=/tmp/zukan-profile --no-first-run about:blank &

# 3) 試験（作成・追加・削除・欠番・写真・5回リロード の8項目）
node tools/reload-test.mjs 9333 run /path/to/なにかの写真.jpg
```

`node tools/reload-test.mjs 9333 verify` は、何も書かずに残っている中身だけを読む。
ブラウザを終了して起動し直したあとの確認に使う。

## 公開する

**本番： https://shirama-y.github.io/mitsuketa-zukan/**

GitHub Pages で `main` をそのまま配信している。**`main` に送れば1〜2分で自動的に
反映される。** 別途の公開操作は要らない。アカウント不要で、どの端末からでも開ける。
会場のタブレットで使うのはこちら。

```
直す → コミット → main に送る → 自動で反映
```

### claude.ai のアーティファクト（試作の確認用）

手元の確認用に、claude.ai のアーティファクトとしても置いてある。こちらは手動更新。
アーティファクトは `<html>` `<head>` `<body>` を自前で付けるので、そのぶんを外して渡す。

```sh
python3 tools/build-artifact.py index.html artifact.html
```

できた `artifact.html` を、既存のアーティファクトと同じ URL へ上書き公開する。

## ふたりで進めるとき

はじめての人は、先に **[共同作業のてびき](https://claude.ai/artifact/TS6arWrM6yJ6a3J3xqpasa)** を読んでほしい。
GitHub が何なのか、Claude Code に日本語でどう頼めばよいかを、そこにまとめてある。

- 直す前に `git pull`。作業は `main` に直接ではなく、枝を切ってから
- `CLAUDE.md` は設計の記録。判断を変えたら、コードと一緒に直す
- 見た目を変えたら、幅 375px と 430px の両方で見る。スマホ縦が主戦場
- 保存・番号・移行に触ったら、上の試験を通してから上げる

## まだ手つかず

- 印刷（持ち帰りの手段がこれしかないので、優先度は高い）
- 外部フォントが読み込めないときの見え方の確認
- 静的サイトとしての配置先（無料枠のどこに置くか未定）
