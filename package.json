{
  "name": "mitsuketa-zukan-native",
  "version": "1.0.0",
  "private": true,
  "description": "Native wrapper for わたしの図鑑ライブラリ",
  "scripts": {
    "build:native": "node scripts/build-native.mjs",
    "native:prepare": "npm run build:native && npx cap sync",
    "native:ios:add": "npm run build:native && npx cap add ios && node scripts/configure-ios.mjs && npx cap sync ios",
    "native:ios:open": "npm run native:ios:sync && npx cap open ios",
    "native:android:add": "npm run build:native && npx cap add android && npx cap sync android",
    "native:android:open": "npm run native:android:sync && npx cap open android",
    "native:ios:sync": "npm run build:native && npx cap sync ios && node scripts/configure-ios.mjs",
    "native:android:sync": "npm run build:native && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/camera": "^8.0.0",
    "@capacitor/share": "^8.0.0",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor/filesystem": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "esbuild": "^0.25.0"
  }
}
