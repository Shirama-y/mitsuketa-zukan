# みつけた図鑑

子どもが自分で図鑑をつくり、見つけたものを写真と言葉で記録していくアプリ。
石でも、車でも、花でも、レゴでも、ペットボトルのキャップでもよい。
図鑑のタイトルは子どもが自分で決める（くるま図鑑、キャップ図鑑、など）。
ひとつの端末に、図鑑をいくつでも作れる。

利用場面は DODOLABO の活動の中で子どもが実際に触れること。
方針の改訂は 2026年9月21日。

## 設計の考え方

正しい名前を調べる科学図鑑ではない。よく見て気づく力を育てる「観察のトレーニング」
として位置づける。

写真から種類を言い当てることは専門家でも難しく、生成AIは誤った名前を確信をもって
提示する。子どもに対してそれを行う必要はない。正解を教える機械ではなく、気づいた
ことを残す場所として設計する。

名前をつける行為そのものが観察であり、「ざらざらぼし」と名付けた子は、その石の
ざらざらを見たことになる。

観察欄と想像欄を分けるのは、事実と想像を区別する練習を記録の形式に埋め込むため。
この2欄は統合しない。

## 画面の構成

1. **本だな** — 作った図鑑が並ぶ。「あたらしい ずかんを つくる」からタイトルを
   入力して作成する
2. **図鑑の中** — 本のページ。1ページ 3×3 の9マスで、罫で仕切る。マスの位置は
   番号で決まる（1ページめ 001〜009、2ページめ 010〜018）。ページは ‹ › でめくる。
   写真の入ったマス、＋ の空きマス、消した番号の空きマス（薄く、押せない）の3種。
   次に埋まるマスだけ図鑑の色で示す。どの空きマスを押しても、入るのは次の番号
   
   ページの上には表紙の柄を細い帯にして通し、罫の色も図鑑の色にする。
   見出しの札は表紙と同じ「かざり」を使う。外と中をそろえるため
3. **記録ページ** — 写真（テープで貼った形）、番号と名前の札、見つけた日、
   見つけたところ、見てわかったこと、想像したこと。「へんしゅう」と「この ページを けす」

図鑑をつくる画面、記録するときの質問、記録をなおす画面は、前面に出るシートで動く。

入れていないもの：下部ナビ、設定、図鑑のマスの上限とページ送り。
下部ナビと設定は、つながる機能がまだないため。上限は今は設けない方針のため

画面はハッシュで切り替える（`#/`、`#/b/<図鑑id>`、`#/b/<図鑑id>/e/<番号>`）。
iPhone の戻る操作を効かせるため。

## 見た目

本物の図鑑を手にしたときの、ぎっしり詰まった楽しさを目指す。

- 明るい紙の上に、柄の図鑑と写真が並ぶ、という見え方を狙う。背景は脇役にして、
  表紙・写真・文字を主役にする
- 地は明るいアイボリー。紙のごく薄いムラだけを残し、強いきめや格子は入れない
- 図鑑の紙は生成り、シート（つくる画面・質問の画面）は白〜オフホワイト、
  入力欄は少し濃いクリーム。影は机から紙が浮く程度にとどめる
- 暗いテーマは持たない。「明るい紙の図鑑」という一つの見え方に決めている。
  図鑑の紙やシールを常に明るく保つ一方で地だけ暗くすると、ちぐはぐになるため
- いろの出どころは `index.html` の `:root` 1か所。下地 / 文字と罫 / アクセント /
  紙の上で使う色 / 物体 / 影 の6組に分けてある
- 図鑑の中は二重の飾り罫と、上部の白い札の見出し
- 図鑑の紙は四隅をテープで留めた形。幅は560pxまでにして、本のページの比率を保つ
- 記録ページの「見て わかったこと」と「そうぞうした こと」は囲みを分け、
  青と黄の点で見分けられるようにする
- 記録は白いフチのシール。番号から決まる角度だけ傾ける。
  重なると押しにくいので、ずらしと重なり順は入れない
- 空き枠は破線の輪郭だけにして、埋まると白フチのシールに変わる
- 白い札とシールは物体として扱い、紙より白く保つ
- 図鑑ごとに色の組み合わせ・柄・タイトルの かざり を持つ。作るときに、12の色と
  8つの柄と4つの かざり（ギザギザ / かざりわく / シンプル / ライン）から選ぶ。
  `frame` を持たない図鑑は 0（ギザギザ）になるので、移行はいらない。
  どちらも見本を見て選べるので、文字が読めなくても選べる

  色の組み合わせは「主役の色 / その淡い色 / 差し色」の3色。似て見えないよう、
  組ごとに主役の色相を変え、差し色も組ごとに変える（以前は全部に同じ水色か黄色を
  入れていて、並べると見分けにくかった）。
  みずいろ / きいろ / みどり / あお / だいだい / あか / ももいろ / ラベンダー / ミント /
  きなり / セージ / あさぎ。全体にくすませて、紙になじむトーンにしている。
  ももいろ・ラベンダー は鮮やかな黄色と組ませない。

  **`COVERS` と `PATTERNS` の並び順が、保存されている `color` と `theme` の番号そのもの。**
  足すときは必ず末尾に置く。並べ替えると、作った図鑑の見た目が変わってしまう

  | 柄 | 中身 |
  | --- | --- |
  | ストライプ | 太い帯・細い線・中くらいの帯 をたてにくり返す |
  | みずたま | 大小2種のまるい点を ずらして置く |
  | ギンガム | 同じ幅の帯を たてよこに半透明で重ねる |
  | こもん | 小さな菱と点を ならべる |
  | よこしま | 太い帯と細い線を よこにくり返す |
  | アーガイル | ひし形を交互に敷き、破線のななめ線を交差させる |
  | タータン | 幅の違う帯を、たてよこ両方に半透明で重ねる |
  | シンプル | 一色の地に、太い帯を一本と細い罫を二本 |

  柄の位相だけ図鑑のidからずらす。開き直しても同じ表紙になる。
  半透明の楕円を重ねて出していた「モヤ」は廃止した
- 色面の上の文字は、代表色ごとに白か濃色かを決める（`COVERS` の `ink`）。
  水色・黄色・だいだい は白文字だとコントラストが足りない
- 題字は Zen Maru Gothic の 900。丸みは出るが、図鑑らしい重さは残る。
  子どもの書いた言葉は Klee One、画面の文字は Zen Kaku Gothic New。
  印刷された題字と本人の字を書き分ける
- 図鑑づくりは「儀式」にする。まっさらな表紙が出て、打った文字がその場で表紙に載り、
  色と柄を選ぶと表紙が変わる。「つくる」で本がひらく（560ms）
- はじめての1件（`no === 1`）だけは特別に祝う。シールが貼られる動きと紙吹雪。
  そのあとは 5・10・20・30・50・100 こ目で小さく知らせる
- 空きマスには、写真が入る場所のかげを置く
- 演出はすべて `prefers-reduced-motion` で止まる
- 記録するときは、質問を1問ずつ出す。上から 進み具合 / 写真 / 質問 / 選択肢 /
  入力欄 / ボタン の縦一列。写真は横幅いっぱいに近く、質問は画面で一番大きい。
  質問は `QUESTIONS` の1か所にまとまっていて、足すのも並べ替えるのも配列で行う
- かたちの質問は、まる・しかく・さんかく・ながい・とがってる・でこぼこ を
  抽象的な図形のカードで示す。選んだものは青い枠とチェック印
- 選択肢のある質問には「あてはまるものを えらんでね（いくつでも えらべます）」を添える
- 色の質問は12個を4つずつ3段。複数選べて、選んだものは太い縁取りと
  濃い丸のチェック印で示す。くろ・しろ・はいいろ が紙に溶けないよう、
  全部の丸に細い縁取りを入れる
- 文言はひらがな多めで、子どもが読める形にする
- 特定のゲームや市販品の意匠は参照しない。柄・配色・書体は独自に決める

## データ

端末内の IndexedDB に保存する。

- データベース名は `ishi-no-wakusei`、version 4。名前は旧方針（石の惑星図鑑）の
  ままで実態と合っていないが、名前を変えると移行が2つのデータベースにまたがり
  壊れやすくなるため据え置いている
- 保存の窓口は `store`。画面は `store.books()` のようにここだけを呼び、IndexedDB を
  直接さわらない。将来サーバーへ移すときは、この中身を差し替えれば済む
- **IndexedDB が正本。** 画面は描くたびに IndexedDB から読み直し、メモリ上に図鑑や記録の
  配列を持たない。起動時に行うのは「開いて読む」だけで、データを初期化・消去する処理は
  どこにもない。消えるのは、利用者が「消す」を押したときだけ
- **開く処理で諦めない。** 開けずに失敗すると、データは残っているのに本だなが空に見える
  （2026年9月22日に実際に起きた）。次の3つを守る
  - 保存されている DB がページより新しい版なら（`VersionError`）、版を指定せずに開き直す。
    古い版のページが表示されているときに起きる。版を下げたり消したりはしない
  - `onblocked` は失敗ではない。別の場所の古い接続が閉じるのを待っているだけで、要求は
    生きている。待っているあいだは案内を出し、成功を待つ
  - `onversionchange` を受けたら接続を閉じる。持ったままだと、新しい版のページが開けずに
    止まる。閉じたら「よみこみなおす」の案内を出す
- 起動時に `navigator.storage.persist()` で、容量が足りないときに消さないよう頼む
- `#/storage` は保存場所の診断ページ。記録が残らない端末で、ばしょ（origin）、うめこみ表示か、
  DB の版、図鑑と記録の数、長く残す設定を確かめる。子どもの画面からはたどれない
- ストア
  - `books` — `{id, title, color, theme, createdAt, nextNo, count}`。
    `color` は色の組（0〜11）、`theme` は柄（0〜7）、`frame` はタイトルの かざり（0〜3）。
    `COVERS` / `PATTERNS` / `FRAMES` の並び順が番号そのものなので、並べ替えず末尾に足す
  - `entries` — `{id, bookId, no, name, date, place, observed, imagined,
    photoBuf, photoType, createdAt}`、`bookId` にインデックス
  - `planets` — 旧方針の単一図鑑。移行元として残してあり、読み書きはしない
- 通し番号は図鑑ごと。`books.nextNo` から採番し、`entries.no` に保存して固定する。
  採番と `nextNo` の更新は同一トランザクションで行う
- 記録を消しても他の番号は変わらない。消えた番号は欠番のままにする（詰めない）
- 記録をなおせるのは、なまえ・見つけた日・見つけた ところ・見て わかったこと・
  そうぞうした こと。番号・写真・図鑑は変えない。「見て わかったこと」を手で
  書きかえたら、もとの答え（`answers`）は文と合わなくなるので外す
- 色と柄の番号の置きかえ（`restyle`）。記録には触らない
  - version 3：むらさき→あお、ピンク→だいだい、ザクザク/ふわふわ/キラキラ→柄0
  - version 4：番号5 が ふかみどり から あか に変わったので、ふかみどり→みどり
  - 置きかえは1回のカーソルでまとめて行う。同じストアに2本走らせると、片方の
    書きこみをもう片方が古い値で上書きしうる
  - 柄0 はグリッドからストライプに替えた。番号は同じなので、グリッドだった図鑑は
    データを変えずにストライプになる
- 旧データは version 2 へのアップグレード時に「はじめての図鑑」へ複写し、
  `createdAt` 昇順で1から番号を振る。アップグレードのトランザクション内で完結する
  ため、失敗すればバージョンは上がらず元のまま残る
- 写真は Blob のまま保存しない。ArrayBuffer に変換して `photoBuf` に入れ、表示時に
  Blob へ戻す。iOS Safari が Blob の保存に失敗するため
- 写真は保存時に圧縮する（長辺900px / JPEG 品質0.75）。容量が最初に上限へ当たる要素
- 記録数は `books.count` に持ち、`entries` の `bookId` インデックスからも数えられる。
  将来ページ数で区切る場合は `no` から算出できる。今回は上限を設けない

データが端末内に閉じるため、その端末を離れると記録は参照できない。これは制約である
と同時に、保管にまつわる問題を丸ごと回避する手段でもある。記録が消えうることは
仕様であって、直すべき不具合ではない。

## 技術方針

運用コストをゼロに保つことを制約条件として設計する。

- 実行時にAIを呼ばない
- 静的サイトとして公開し、無料枠に配置する。
  **配置先は GitHub Pages（https://shirama-y.github.io/mitsuketa-zukan/）。**
  `main` に送ると自動で反映される。無料で使うためリポジトリは公開にしてある。
  アプリが1枚のHTMLである以上、公開すればソースは誰でも読めるので、
  リポジトリを非公開に保つ意味は薄いという判断（2026年9月25日）
- 写真の撮影と記録だけで、アプリ単体で完結させる
- 外部フォントが読み込めなくても表示が崩れないようにする
- 開発は Claude Code（CLI）で行う

将来AIを「ヒント」ボタンとして追加する余地は残す。排除は恒久的な決定ではない。

## 含めないもの

| 対象 | 理由 |
| --- | --- |
| 共有機能、サーバーとの通信 | 保管サービスが必要になり、無料枠との戦いが始まる。Artifact の `db` / `assets` も、宣言すると組織内限定になり、閲覧者全員に Claude のサインインを求めるので採らない |
| アカウント機能 | 上記に同じ |
| 課金、ページ数の上限 | 現時点で判断する材料がない |
| イベント用の機能（投影、一斉公開など） | 初版の範囲外 |
| AIによる判定・説明 | 実行時に従量課金が発生する。判定精度そのものにも難がある |

## 未決事項

- 文字がまだ書けない年齢の子どもへの対応（音声入力か、選択肢から選ぶ方式か）
- 記録項目の最終決定（項目を増やすか、さらに削るか）
- 印刷・持ち帰りの形
- AIを「ヒント」として追加する場合の位置づけと費用負担

いずれも実際に子どもに触らせてみないと判断できない。シンプルに作り、現場で
削ったり足したりする。

## やりとり

返答は日本語。
